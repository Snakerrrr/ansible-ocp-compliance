# Conexión Hub: ruta al kubeconfig del Hub (opcional).
# Si no se define, se usa la variable de entorno KUBECONFIG inyectada por AAP (credencial OpenShift/Kubernetes).
hub_kubeconfig_path: ""

# Configuración de Correo
send_mail: true
smtp_host: "smtp.gmail.com"

# Reporte
report_dir: "/tmp/ocp-reports"
report_file: "reporte-seguridad-{{ lookup('pipe', 'date +%Y%m%d') }}"
date: "{{ lookup('pipe', 'date +%Y%m%d-%H%M') }}"

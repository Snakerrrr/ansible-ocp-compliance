---
# Playbook Unificado: compliance-pipeline.yml
# ENTRYPOINT único para toda la automatización de compliance OCP
#
# Este playbook es 100% agnóstico - NO contiene valores hardcodeados.
# Todas las variables deben inyectarse desde AAP (Extra Vars, Survey o Credentials).
#
# Flags de control:
#   do_gitops: true|false  - Ejecutar GitOps (toggle + commit + push)
#   do_export_html: true|false - Exportar resultados a HTML
#   do_send_email: true|false - Enviar reportes por correo

- name: Compliance Pipeline OCP (CIS + PCI DSS)
  hosts: localhost
  gather_facts: true

  vars:
    # --- CONFIGURACIÓN ESTRUCTURAL (Sin datos de usuario) ---
    # Solo dejamos rutas temporales que no dependen del usuario
    export_output_dir: "/tmp/compliance-reports"
    gitops_repo_path: "/tmp/acm-policies"  # Ruta fija temporal, sin recursión
    
    # Defaults de seguridad (para que no falle si no se pasan)
    do_gitops: false
    do_export_html: false
    do_send_email: false

    # YA NO definimos github_user, github_token, email_smtp_host, etc. aquí.
    # Ansible esperará que vengan de AAP (Extra Vars, Survey o Credentials).

  tasks:
    # ============================================================
    # VALIDACIÓN DE INPUTS (Semáforo de Seguridad)
    # ============================================================
    - name: Validar variables obligatorias de infraestructura Git
      assert:
        that:
          - github_user is defined
          - github_user | length > 0
          - gitops_repo_branch is defined
          - gitops_repo_branch | length > 0
          - github_token is defined
          - github_token | length > 0
        fail_msg: "❌ ERROR: Faltan variables de Git. Defínalas en AAP (Extra Vars, Survey o Credential de Source Control)."
      when: do_gitops | bool

    - name: Validar variables obligatorias de correo
      assert:
        that:
          - email_smtp_host is defined
          - email_smtp_host | length > 0
          - email_smtp_username is defined
          - email_smtp_username | length > 0
          - email_smtp_password is defined
          - email_smtp_password | length > 0
          - email_to is defined
          - email_to | length > 0
        fail_msg: "❌ ERROR: Faltan variables de Correo. Defínalas en AAP (Extra Vars o Survey)."
      when: do_send_email | bool

    - name: Validar flags de control
      fail:
        msg: "Al menos uno de los flags debe estar activado: do_gitops o do_export_html"
      when: not do_gitops and not do_export_html

    # ============================================================
    # CONFIGURACIÓN Y DEBUG INICIAL
    # ============================================================
    - name: Establecer directorio de salida con nombre de cluster (multicluster)
      set_fact:
        export_output_dir: "{{ (export_output_dir | default('/tmp/compliance-reports')) + '/' + target_cluster_context if (target_cluster_context is defined and target_cluster_context != '') else (export_output_dir | default('/tmp/compliance-reports')) }}"

    - name: Mostrar configuración recibida (Sin secretos)
      debug:
        msg: |
          🚀 INICIO DE PIPELINE DE COMPLIANCE
          ====================================
          GitOps:         {{ do_gitops }}
          Export HTML:    {{ do_export_html }}
          Enviar Correo:  {{ do_send_email }}
          User Git:       {{ github_user if (do_gitops and github_user is defined) else 'N/A' }}
          Branch:         {{ gitops_repo_branch if (do_gitops and gitops_repo_branch is defined) else 'N/A' }}
          Cluster:        {{ target_cluster_context if (target_cluster_context is defined and target_cluster_context != '') else 'HUB (contexto actual)' }}
          Destinatario:   {{ email_to if (do_send_email and email_to is defined) else 'N/A' }}
          Host SMTP:      {{ email_smtp_host if (do_send_email and email_smtp_host is defined) else 'N/A' }}
          ====================================

    # ============================================================
    # FASE 1: GitOps (toggle policies + commit + push)
    # ============================================================
    - name: Ejecutar GitOps (toggle + commit + push)
      when: do_gitops | bool
      block:
        - name: Ejecutar rol gitops_policy_update
          include_role:
            name: gitops_policy_update
          vars:
            github_user: "{{ github_user }}"
            github_token: "{{ github_token }}"
            gitops_repo_path: "{{ gitops_repo_path | default('/tmp/acm-policies') }}"
            gitops_repo_branch: "{{ gitops_repo_branch }}"
            run_cis: "{{ run_cis | default(true) }}"
            run_pci: "{{ run_pci | default(false) }}"
            scan_remediation_action: "{{ scan_remediation_action | default('inform') }}"
            install_operator_remediation_action: "{{ install_operator_remediation_action | default('enforce') }}"
            placement_label_key: "{{ placement_label_key | default('compliance') }}"
            placement_label_value: "{{ placement_label_value | default('enabled') }}"
            placement_use_matchlabels: "{{ placement_use_matchlabels | default(true) }}"
            scan_schedule: "{{ scan_schedule | default('0 2 * * *') }}"
            scan_setting_name: "{{ scan_setting_name | default('periodic-daily') }}"
            cis_scan_enabled: "{{ run_cis | default(true) }}"
            pci_scan_enabled: "{{ run_pci | default(false) }}"

        - name: Informar que GitOps se completó
          debug:
            msg: "GitOps completado. ArgoCD sincronizará automáticamente los cambios."

    # ============================================================
    # FASE 2: Esperar instalación y completación (nuevos clusters)
    # ============================================================
    - name: Esperar instalación del operador y completación de scans
      when: do_gitops | bool and do_export_html | bool
      block:
        - name: Ejecutar rol compliance_wait
          include_role:
            name: compliance_wait
          vars:
            target_cluster_context: "{{ target_cluster_context if (target_cluster_context is defined and target_cluster_context != '') else '' }}"

        - name: Informar que la espera se completó
          debug:
            msg: "Espera completada. El operador está instalado y los scans han finalizado."

    # ============================================================
    # FASE 3: Export HTML (healthcheck informativo)
    # ============================================================
    - name: Ejecutar exportación HTML
      when: do_export_html | bool
      block:
        - name: Ejecutar rol compliance_export_html
          include_role:
            name: compliance_export_html
          vars:
            export_output_dir: "{{ export_output_dir }}"
            target_cluster_context: "{{ target_cluster_context if (target_cluster_context is defined and target_cluster_context != '') else '' }}"
            managed_cluster_kubeconfigs: "{{ managed_cluster_kubeconfigs | default({}) }}"

        - name: Informar que exportación se completó
          debug:
            msg: "Exportación HTML completada. Reportes disponibles en: {{ export_output_dir }}"

    # ============================================================
    # FASE 4: Envío de reportes por correo electrónico
    # ============================================================
    # NOTA: En AAP, los Pods son efímeros. Los archivos en /tmp se pierden
    # al finalizar el job. Esta fase envía los reportes por correo antes
    # de que el Pod se destruya.
    - name: Enviar reportes por correo electrónico
      when: 
        - do_export_html | bool
        - do_send_email | bool
      block:
        - name: Verificar disponibilidad del módulo mail
          command: python3 -c "import smtplib; import email"
          register: mail_module_check
          changed_when: false
          failed_when: false

        - name: Advertir si el módulo mail no está disponible
          debug:
            msg: |
              ⚠️  ADVERTENCIA: El módulo mail de Ansible no está disponible.
              
              El módulo mail requiere las librerías Python:
              - smtplib (normalmente incluida en Python estándar)
              - email (normalmente incluida en Python estándar)
              
              En AAP, asegúrate de que tu Execution Environment tenga Python
              con estas librerías disponibles.
              
              El envío de correo se omitirá.
          when: mail_module_check.rc != 0

        - name: Establecer email_from si no está definido
          set_fact:
            email_from: "{{ email_smtp_username }}"
          when: 
            - mail_module_check.rc == 0
            - email_from is not defined or email_from == ""

        - name: Establecer email_subject_prefix si no está definido
          set_fact:
            email_subject_prefix: "{{ email_subject_prefix | default('Reporte de Compliance') }}"
          when: mail_module_check.rc == 0

        - name: Buscar archivo ZIP generado
          find:
            paths: "{{ export_output_dir }}"
            patterns: "compliance-reports-*.zip"
            file_type: file
          register: zip_files
          failed_when: false
          when: mail_module_check.rc == 0

        - name: Buscar archivo summary.txt
          find:
            paths: "{{ export_output_dir }}"
            patterns: "summary.txt"
            file_type: file
          register: summary_files
          failed_when: false
          when: mail_module_check.rc == 0

        - name: Determinar archivo ZIP a enviar
          set_fact:
            zip_file_to_send: "{{ zip_files.files[0].path if (zip_files.files is defined and zip_files.files | length > 0) else '' }}"
            summary_file_to_send: "{{ summary_files.files[0].path if (summary_files.files is defined and summary_files.files | length > 0) else '' }}"
          when: mail_module_check.rc == 0

        - name: Advertir si no se encontró archivo ZIP
          debug:
            msg: |
              ⚠️  No se encontró archivo ZIP en {{ export_output_dir }}.
              El correo se enviará sin adjuntos.
          when: 
            - mail_module_check.rc == 0
            - zip_file_to_send == ""

        - name: Construir asunto del correo
          set_fact:
            email_subject: "{{ email_subject_prefix }} - {{ target_cluster_context if (target_cluster_context is defined and target_cluster_context != '') else 'HUB' }} - {{ ansible_date_time.date }}"
          when: mail_module_check.rc == 0

        - name: Construir cuerpo del correo
          set_fact:
            email_body: |
              Reporte de Compliance generado exitosamente.
              
              Detalles:
              - Cluster: {{ target_cluster_context if (target_cluster_context is defined and target_cluster_context != '') else 'HUB (contexto actual)' }}
              - Fecha: {{ ansible_date_time.iso8601 }}
              - Directorio: {{ export_output_dir }}
              
              {% if zip_file_to_send != '' %}
              - Archivo ZIP adjunto: {{ zip_file_to_send | basename }}
              {% endif %}
              
              {% if summary_file_to_send != '' %}
              - Summary adjunto: {{ summary_file_to_send | basename }}
              {% endif %}
              
              Este correo fue generado automáticamente por el Compliance Pipeline de Ansible.
          when: mail_module_check.rc == 0

        - name: Enviar reporte por correo (con ZIP si existe)
          mail:
            host: "{{ email_smtp_host }}"
            port: "{{ email_smtp_port | default(587) | int }}"
            username: "{{ email_smtp_username }}"
            password: "{{ email_smtp_password }}"
            to: "{{ email_to }}"
            from: "{{ email_from }}"
            subject: "{{ email_subject }}"
            body: "{{ email_body }}"
            attach:
              - "{{ zip_file_to_send }}"
            secure: starttls
          when: 
            - mail_module_check.rc == 0
            - zip_file_to_send != ""
          ignore_errors: true
          register: email_result_zip

        - name: Enviar reporte por correo (solo summary si no hay ZIP)
          mail:
            host: "{{ email_smtp_host }}"
            port: "{{ email_smtp_port | default(587) | int }}"
            username: "{{ email_smtp_username }}"
            password: "{{ email_smtp_password }}"
            to: "{{ email_to }}"
            from: "{{ email_from }}"
            subject: "{{ email_subject }}"
            body: "{{ email_body }}"
            attach:
              - "{{ summary_file_to_send }}"
            secure: starttls
          when: 
            - mail_module_check.rc == 0
            - zip_file_to_send == ""
            - summary_file_to_send != ""
          ignore_errors: true
          register: email_result_summary

        - name: Enviar reporte por correo (sin adjuntos si no hay archivos)
          mail:
            host: "{{ email_smtp_host }}"
            port: "{{ email_smtp_port | default(587) | int }}"
            username: "{{ email_smtp_username }}"
            password: "{{ email_smtp_password }}"
            to: "{{ email_to }}"
            from: "{{ email_from }}"
            subject: "{{ email_subject }}"
            body: "{{ email_body }}"
            secure: starttls
          when: 
            - mail_module_check.rc == 0
            - zip_file_to_send == ""
            - summary_file_to_send == ""
          ignore_errors: true
          register: email_result_no_attach

        - name: Verificar resultado del envío de correo
          set_fact:
            email_sent_successfully: >
              (email_result_zip is defined and email_result_zip.failed == false) or
              (email_result_summary is defined and email_result_summary.failed == false) or
              (email_result_no_attach is defined and email_result_no_attach.failed == false)
          when: mail_module_check.rc == 0

        - name: Informar que correo se envió exitosamente
          debug:
            msg: |
              ✅ Correo enviado exitosamente a {{ email_to }}
              Asunto: {{ email_subject }}
              {% if zip_file_to_send != '' %}
              Archivo adjunto: {{ zip_file_to_send | basename }}
              {% elif summary_file_to_send != '' %}
              Archivo adjunto: {{ summary_file_to_send | basename }}
              {% endif %}
          when: 
            - mail_module_check.rc == 0
            - email_sent_successfully | default(false) | bool

        - name: Advertir si el envío de correo falló
          debug:
            msg: |
              ⚠️  El envío de correo falló. Verifica:
              1. Que el módulo mail esté disponible en el Execution Environment
              2. Que las credenciales SMTP sean correctas
              3. Que el servidor SMTP sea accesible desde AAP
              4. Que el puerto SMTP no esté bloqueado por firewall
              
              Los reportes están disponibles en: {{ export_output_dir }}
              (Nota: En AAP, estos archivos se perderán cuando el Pod termine)
          when: 
            - mail_module_check.rc == 0
            - not (email_sent_successfully | default(false) | bool)

    # ============================================================
    # Resumen final
    # ============================================================
    - name: Resumen final del pipeline
      debug:
        msg: |
          ========================================
          Compliance Pipeline - Ejecución Completada
          ========================================
          GitOps ejecutado: {{ do_gitops }}
          Export HTML ejecutado: {{ do_export_html }}
          Correo enviado: {{ do_send_email if (do_export_html and do_send_email) else false }}
          ========================================
